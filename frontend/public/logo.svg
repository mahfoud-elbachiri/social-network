<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100" width="100" height="100">
  <!-- 6-part fan/pinwheel logo -->
  <defs>
    <linearGradient id="leafGrad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" style="stop-color:#c3ff00;stop-opacity:1" />
      <stop offset="100%" style="stop-color:#9acd32;stop-opacity:1" />
    </linearGradient>
  </defs>
  
  <!-- 6 fan blades rotating around center -->
  <!-- Blade 1 - 0 degrees -->
  <ellipse cx="50" cy="25" rx="12" ry="22" fill="url(#leafGrad)" transform="rotate(0 50 50)"/>
  
  <!-- Blade 2 - 60 degrees -->
  <ellipse cx="50" cy="25" rx="12" ry="22" fill="url(#leafGrad)" transform="rotate(60 50 50)"/>
  
  <!-- Blade 3 - 120 degrees -->
  <ellipse cx="50" cy="25" rx="12" ry="22" fill="url(#leafGrad)" transform="rotate(120 50 50)"/>
  
  <!-- Blade 4 - 180 degrees -->
  <ellipse cx="50" cy="25" rx="12" ry="22" fill="url(#leafGrad)" transform="rotate(180 50 50)"/>
  
  <!-- Blade 5 - 240 degrees -->
  <ellipse cx="50" cy="25" rx="12" ry="22" fill="url(#leafGrad)" transform="rotate(240 50 50)"/>
  
  <!-- Blade 6 - 300 degrees -->
  <ellipse cx="50" cy="25" rx="12" ry="22" fill="url(#leafGrad)" transform="rotate(300 50 50)"/>
  
  <!-- Center circle -->
  <circle cx="50" cy="50" r="10" fill="#252525"/>
  <circle cx="50" cy="50" r="6" fill="#c3ff00"/>
</svg>
